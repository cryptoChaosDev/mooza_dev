# =============================================================
# MOOZA — шаблон переменных окружения
# Скопируйте этот файл в .env и заполните значения
# =============================================================

# ----- База данных -----
POSTGRES_USER=mooza
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=mooza_db

# ----- Безопасность -----
# Минимум 64 символа. Сгенерировать: openssl rand -hex 64
JWT_SECRET=your_jwt_secret_here

# ----- Сервер -----
PORT=4000
NODE_ENV=development

# ----- CORS -----
# Локально:
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
# На VPS (замените IP на свой):
# ALLOWED_ORIGINS=http://YOUR_VPS_IP,http://YOUR_VPS_IP:3000,http://YOUR_VPS_IP:4000

# ----- Frontend API URL -----
# Локально (по умолчанию, можно не указывать):
VITE_API_URL=http://localhost:4000
# На VPS (замените IP на свой):
# VITE_API_URL=http://YOUR_VPS_IP:4000
