# 🚀 Быстрый старт на VPS

## За 5 минут от нуля до работающего приложения

### Шаг 1: Загрузите проект на VPS

**Вариант А: Через Git (рекомендуется)**
```bash
# На VPS
cd /opt
sudo git clone https://github.com/your-username/mooza.git mooza
sudo chown -R $USER:$USER mooza
cd mooza
```

**Вариант Б: Через SCP/SFTP**
```bash
# На локальной машине
cd c:\Users\79779\Desktop\mooza_dev\mooza_dev
scp -r . user@your-vps-ip:/opt/mooza

# Затем на VPS
cd /opt/mooza
```

### Шаг 2: Сделайте скрипты исполняемыми

```bash
chmod +x *.sh
```

### Шаг 3: Настройте VPS (выполнить ОДИН РАЗ)

```bash
sudo ./setup-vps.sh
```

Этот скрипт установит:
- Docker и Docker Compose
- UFW файрвол
- Git, curl, wget
- Опционально: Nginx и Certbot

⏱️ Время: ~3-5 минут

### Шаг 4: Разверните приложение

```bash
./deploy.sh
```

Скрипт спросит:
1. **IP адрес или домен:** введите IP вашего VPS (например: `123.45.67.89`)
2. **Использовать HTTPS:** ответьте `n` (пока нет SSL)

Скрипт автоматически:
- ✅ Создаст безопасный JWT_SECRET
- ✅ Сгенерирует пароль для PostgreSQL
- ✅ Настроит docker-compose.prod.yml
- ✅ Соберет и запустит контейнеры
- ✅ Проверит работоспособность

⏱️ Время: ~5-10 минут (зависит от скорости интернета)

### Шаг 5: Готово! 🎉

Ваше приложение доступно по адресам:

- **Веб-интерфейс:** `http://YOUR_IP:3000`
- **API:** `http://YOUR_IP:4000`

Попробуйте зарегистрироваться и войти!

---

## 📱 Что дальше?

### Настройте HTTPS (рекомендуется)

1. Убедитесь, что у вас есть домен (например: `mooza.ru`)
2. Настройте DNS: A-запись должна указывать на IP вашего VPS
3. Установите SSL сертификат:

```bash
# Если вы установили Nginx и Certbot через setup-vps.sh
sudo certbot --nginx -d mooza.ru -d www.mooza.ru

# Затем пересоберите приложение с HTTPS
./deploy.sh
# На вопрос "Использовать HTTPS?" ответьте 'y'
# Введите домен: mooza.ru
```

### Настройте автоматические бэкапы

```bash
# Добавьте в crontab
crontab -e

# Вставьте строку (бэкап каждый день в 3:00)
0 3 * * * cd /opt/mooza && ./backup.sh
```

### Просмотр логов

```bash
./logs.sh
```

Выберите нужный сервис из интерактивного меню.

---

## 🔧 Полезные команды

```bash
# Перезапуск всех сервисов
docker-compose -f docker-compose.prod.yml restart

# Остановка
docker-compose -f docker-compose.prod.yml down

# Запуск
docker-compose -f docker-compose.prod.yml up -d

# Статус контейнеров
docker ps

# Проверка API
curl http://localhost:4000/health
```

---

## 🆘 Помощь

### Приложение не работает?

1. **Проверьте логи:**
   ```bash
   ./logs.sh
   ```

2. **Проверьте статус контейнеров:**
   ```bash
   docker ps
   ```

3. **Проверьте файрвол:**
   ```bash
   sudo ufw status
   ```
   Порты 3000 и 4000 должны быть открыты.

### Забыли пароль от PostgreSQL?

Пароль находится в файле `.env`:
```bash
cat .env | grep POSTGRES_PASSWORD
```

### Нужно пересоздать все с нуля?

```bash
# ОСТОРОЖНО: Это удалит все данные!
docker-compose -f docker-compose.prod.yml down -v
./deploy.sh
```

---

## 📚 Документация

- [SCRIPTS_README.md](SCRIPTS_README.md) - Подробное описание всех скриптов
- [DEPLOY.md](DEPLOY.md) - Полная документация по развертыванию

---

## ✅ Чеклист

- [ ] Проект загружен на VPS в `/opt/mooza`
- [ ] Выполнен `sudo ./setup-vps.sh`
- [ ] Выполнен `./deploy.sh`
- [ ] Приложение доступно на `http://YOUR_IP:3000`
- [ ] Регистрация и вход работают
- [ ] (Опционально) Настроен HTTPS
- [ ] (Опционально) Настроены автоматические бэкапы

**Готово!** Ваше приложение работает на VPS! 🎉

---

**Время развертывания:** ~10-15 минут
**Сложность:** ⭐⭐ (из 5)

Mooza Music Social Network 🎵
