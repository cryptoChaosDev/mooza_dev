// Feature flags. Flip to re-enable.
//
// DEALS_ENABLED — deal creation ("Оформить сделку") and the "Деловые" tab in
// Messages are temporarily hidden across the app. Existing deals/chats are not
// deleted; only the entry points are gated. Set to `true` to bring it all back.
export const DEALS_ENABLED: boolean = false;
