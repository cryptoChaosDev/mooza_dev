# 🚀 Скрипты развертывания Mooza

Быстрый справочник по скриптам для развертывания и управления Mooza на VPS.

## 📋 Список скриптов

| Скрипт | Описание | Когда использовать |
|--------|----------|-------------------|
| `setup-vps.sh` | Первоначальная настройка VPS | Один раз при первой установке |
| `deploy.sh` | Развертывание приложения | При первом развертывании и обновлениях |
| `backup.sh` | Создание бэкапа БД | Перед важными изменениями, регулярно |
| `restore.sh` | Восстановление из бэкапа | При необходимости откатить данные |
| `logs.sh` | Просмотр логов | Для отладки и мониторинга |

## 🎯 Быстрый старт

### 1️⃣ Первое развертывание на чистом VPS

```bash
# 1. Загрузите проект
git clone <your-repo> /opt/mooza
cd /opt/mooza

# 2. Сделайте скрипты исполняемыми
chmod +x *.sh

# 3. Настройте VPS (требуется sudo, выполнить один раз)
sudo ./setup-vps.sh

# 4. Разверните приложение
./deploy.sh
```

**Готово!** Приложение будет доступно на портах 3000 (веб) и 4000 (API).

### 2️⃣ Обновление уже развернутого приложения

```bash
cd /opt/mooza

# Скачать изменения
git pull

# Пересобрать и перезапустить
./deploy.sh
```

### 3️⃣ Регулярные бэкапы

```bash
# Создать бэкап
./backup.sh

# Настроить автоматические бэкапы (раз в день в 3:00)
crontab -e
# Добавить: 0 3 * * * cd /opt/mooza && ./backup.sh
```

### 4️⃣ Просмотр логов

```bash
# Интерактивный просмотр
./logs.sh

# Выберите нужный сервис из меню
```

## 📖 Детальное описание скриптов

### `setup-vps.sh` - Настройка VPS

**Что делает:**
- ✅ Обновляет систему
- ✅ Устанавливает Docker и Docker Compose
- ✅ Настраивает UFW файрвол
- ✅ Устанавливает Git, curl, wget и другие утилиты
- ✅ Опционально: Nginx и Certbot для SSL
- ✅ Создает swap (если нужно)
- ✅ Настраивает автоматическую очистку Docker

**Использование:**
```bash
sudo ./setup-vps.sh
```

**Требования:**
- Ubuntu/Debian VPS
- Права root/sudo
- Запускать только один раз при первой настройке

---

### `deploy.sh` - Развертывание приложения

**Что делает:**
- ✅ Проверяет наличие Docker
- ✅ Запрашивает IP/домен сервера
- ✅ Генерирует безопасный JWT_SECRET
- ✅ Генерирует пароль для PostgreSQL
- ✅ Создает правильный docker-compose.prod.yml
- ✅ Обновляет Dockerfile для передачи VITE_API_URL
- ✅ Останавливает старые контейнеры
- ✅ Собирает и запускает новые контейнеры
- ✅ Проверяет работоспособность

**Использование:**
```bash
./deploy.sh
```

**Интерактивные вопросы:**
1. IP адрес или домен сервера (например: `123.45.67.89` или `mooza.ru`)
2. Использовать HTTPS? (yes/no)

**Результат:**
- Файл `.env` с секретами
- Обновленный `docker-compose.prod.yml`
- Запущенные контейнеры

---

### `backup.sh` - Бэкап базы данных

**Что делает:**
- ✅ Создает дамп PostgreSQL базы данных
- ✅ Сохраняет с временной меткой
- ✅ Опционально: сжимает gzip
- ✅ Опционально: удаляет старые бэкапы (>7 дней)

**Использование:**
```bash
./backup.sh
```

**Бэкапы сохраняются в:**
```
./backups/mooza_backup_YYYYMMDD_HHMMSS.sql
./backups/mooza_backup_YYYYMMDD_HHMMSS.sql.gz  (если сжат)
```

**Автоматизация (cron):**
```bash
# Ежедневно в 3:00 ночи
0 3 * * * cd /opt/mooza && ./backup.sh

# Каждые 6 часов
0 */6 * * * cd /opt/mooza && ./backup.sh
```

---

### `restore.sh` - Восстановление БД

**Что делает:**
- ✅ Показывает доступные бэкапы
- ✅ Создает страховочный бэкап текущей БД
- ✅ Восстанавливает выбранный бэкап
- ✅ Поддерживает .sql и .sql.gz файлы

**Использование:**
```bash
./restore.sh
```

**⚠️ ВНИМАНИЕ:**
- Восстановление удалит все текущие данные!
- Скрипт создаст страховочный бэкап перед восстановлением
- Подтверждение требуется (нужно ввести `yes`)

---

### `logs.sh` - Просмотр логов

**Что делает:**
- ✅ Интерактивное меню выбора сервиса
- ✅ Показывает логи выбранного контейнера
- ✅ Режим "follow" для отслеживания в реальном времени

**Использование:**
```bash
./logs.sh
```

**Опции меню:**
1. Все сервисы
2. API (backend)
3. Web (frontend)
4. PostgreSQL
5. Последние 50 строк
6. Следить в реальном времени (Ctrl+C для выхода)

---

## 🔧 Ручные команды Docker

Если нужно выполнить команды вручную:

```bash
# Определите команду compose
DOCKER_COMPOSE="docker compose"  # или "docker-compose"

# Статус контейнеров
docker ps

# Логи
$DOCKER_COMPOSE -f docker-compose.prod.yml logs -f

# Перезапуск
$DOCKER_COMPOSE -f docker-compose.prod.yml restart

# Остановка
$DOCKER_COMPOSE -f docker-compose.prod.yml down

# Пересборка
$DOCKER_COMPOSE -f docker-compose.prod.yml up -d --build

# Вход в контейнер
docker exec -it mooza-api sh
docker exec -it mooza-postgres psql -U mooza -d mooza_db
```

## 🐛 Устранение неполадок

### Скрипт не запускается

```bash
# Проверьте права
ls -l *.sh

# Сделайте исполняемым
chmod +x *.sh

# Проверьте формат конца строк (должен быть Unix)
dos2unix *.sh  # если установлен
```

### Docker не найден

```bash
# Установите заново
sudo ./setup-vps.sh
```

### Контейнеры не запускаются

```bash
# Проверьте логи
./logs.sh

# Попробуйте пересоздать
docker-compose -f docker-compose.prod.yml down
./deploy.sh
```

### Ошибки базы данных

```bash
# Проверьте статус PostgreSQL
docker exec mooza-postgres pg_isready -U mooza

# Проверьте логи
docker-compose -f docker-compose.prod.yml logs postgres

# В крайнем случае: пересоздайте (ПОТЕРЯ ДАННЫХ!)
docker-compose -f docker-compose.prod.yml down -v
./deploy.sh
```

## 📚 Дополнительная информация

Полная документация: [DEPLOY.md](DEPLOY.md)

## ✅ Чеклист production-готовности

- [ ] Запущен `setup-vps.sh`
- [ ] Настроен UFW файрвол
- [ ] Запущен `deploy.sh` с правильным IP/доменом
- [ ] JWT_SECRET сгенерирован (в .env)
- [ ] Пароль PostgreSQL изменен
- [ ] Настроен Nginx reverse proxy
- [ ] Настроен SSL (HTTPS)
- [ ] Настроены автоматические бэкапы (cron)
- [ ] Проверена работа входа/регистрации
- [ ] Проверены логи на ошибки

---

**Mooza Music Social Network** 🎵
Developed with ❤️
