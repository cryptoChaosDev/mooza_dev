# 🪟 Развертывание с Windows на VPS

## Важно!
Скрипты `.sh` работают только на Linux. На Windows вы работаете с кодом, а развертывание происходит на VPS.

## 🚀 Процесс развертывания с Windows

### Шаг 1: Подготовьте код на Windows

```bash
# В Git Bash или PowerShell
cd c:\Users\79779\Desktop\mooza_dev\mooza_dev

# Проверьте, что все файлы созданы
ls *.sh
```

Должны быть файлы:
- `setup-vps.sh`
- `deploy.sh`
- `backup.sh`
- `restore.sh`
- `logs.sh`

### Шаг 2: Закоммитьте изменения в Git

```bash
# Добавьте новые файлы
git add .

# Создайте коммит
git commit -m "Add VPS deployment scripts and fixes"

# Загрузите на GitHub/GitLab
git push origin main
```

### Шаг 3: Подключитесь к VPS

**Вариант А: Через PowerShell/CMD**
```bash
ssh your-username@your-vps-ip
```

**Вариант Б: Через PuTTY**
1. Откройте PuTTY
2. Введите IP адрес VPS
3. Нажмите "Open"
4. Введите логин и пароль

### Шаг 4: На VPS - клонируйте проект

```bash
# На VPS (после подключения по SSH)
cd /opt
sudo git clone https://github.com/your-username/mooza.git mooza

# Или если репозиторий уже есть - обновите
cd /opt/mooza
sudo git pull

# Дайте себе права
sudo chown -R $USER:$USER /opt/mooza
```

### Шаг 5: На VPS - запустите скрипты

```bash
# Сделайте скрипты исполняемыми
chmod +x *.sh

# Первоначальная настройка VPS (один раз)
sudo ./setup-vps.sh

# Развертывание приложения
./deploy.sh
```

Скрипт `deploy.sh` спросит:
- **IP адрес или домен:** введите IP вашего VPS
- **HTTPS:** ответьте `n` (пока нет SSL)

### Шаг 6: Готово!

Приложение доступно по адресам:
- Веб: `http://YOUR_VPS_IP:3000`
- API: `http://YOUR_VPS_IP:4000`

---

## 📁 Альтернатива: Передача файлов через SCP

Если не используете Git:

### Через PowerShell/CMD:

```bash
# Передать весь проект на VPS
scp -r c:\Users\79779\Desktop\mooza_dev\mooza_dev your-username@your-vps-ip:/opt/mooza
```

### Через WinSCP (GUI):

1. Скачайте WinSCP: https://winscp.net/
2. Подключитесь к VPS
3. Перетащите папку проекта в `/opt/mooza`

Затем на VPS:
```bash
cd /opt/mooza
chmod +x *.sh
sudo ./setup-vps.sh
./deploy.sh
```

---

## 🔧 Локальная разработка на Windows

Для локальной разработки используйте Docker Desktop:

```bash
# Запуск для разработки (не production!)
docker-compose up -d

# Остановка
docker-compose down
```

---

## ❓ Частые вопросы

### Q: Как запустить .sh файлы на Windows?
**A:** Никак! Скрипты `.sh` работают только на Linux (VPS). На Windows вы работаете с кодом и используете Git для загрузки на VPS.

### Q: Могу ли я тестировать локально на Windows?
**A:** Да, используйте `docker-compose.yml` (не `.prod.yml`):
```bash
docker-compose up -d
```

### Q: Как обновить код на VPS?
**A:**
1. На Windows: `git push`
2. На VPS:
```bash
cd /opt/mooza
git pull
./deploy.sh
```

### Q: Забыл пароль от VPS
**A:** Восстановите через панель управления вашего хостинг-провайдера.

### Q: Как посмотреть логи на VPS?
**A:** Подключитесь по SSH и запустите:
```bash
cd /opt/mooza
./logs.sh
```

---

## 🎯 Краткая шпаргалка

### На Windows:
```bash
git add .
git commit -m "Update"
git push
```

### На VPS:
```bash
cd /opt/mooza
git pull
./deploy.sh
```

**Всё!** 🎉

---

## 📚 Дополнительные ссылки

- [VPS_QUICK_START.md](VPS_QUICK_START.md) - Быстрый старт на VPS
- [SCRIPTS_README.md](SCRIPTS_README.md) - Описание всех скриптов
- [DEPLOY.md](DEPLOY.md) - Полная документация

---

**Mooza Music Social Network** 🎵
